name: foundation
description: A new Flutter package.
publish_to: 'none'

environment:
  sdk: '^3.5.3'

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  assets:
    path: ../assets
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: